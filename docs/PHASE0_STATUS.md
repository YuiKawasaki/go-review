# Phase 0 の進捗

## 完了（2026-08-20）

| 項目 | 状態 |
|---|---|
| 囲碁 棋譜DB の作成（プロパティ13件・ビュー3件） | 完了 |
| 囲碁 学習ログDB の作成（プロパティ7件） | 完了 |
| 移行元（日報DB）の特定 | 完了 |
| `.env` の作成（トークン以外すべて記入済み） | 完了 |
| 実データ3局でのパーサ検証 | 完了・すべて期待どおり |
| 移行の本実行（`--execute`） | 完了・11件移行 / エラー0件 / 重複0件 |

### 控えたID

| 変数 | 値 |
|---|---|
| `KIFU_DB_ID` | `078cb5f3-03b6-4ea2-b2dc-b5e8c4a1121d` |
| `KIFU_DS_ID` | `20e08d38-8698-4961-930b-7488be06e0c1` |
| `LOG_DB_ID` | `856d05a3-e2c1-4613-91b7-c6ad2fa013f4` |
| `LOG_DS_ID` | `6c31f877-6d08-417f-a815-72ba8b662f05` |
| `OLD_DS_ID`（日報DB） | `1fad440e-bdfa-8076-9e37-000bc95fa9ce` |

- 棋譜DB: https://app.notion.com/p/078cb5f303b64ea2b2dcb5e8c4a1121d
- 学習ログDB: https://app.notion.com/p/856d05a3e2c1461391b7c6ad2fa013f4

### 実データ検証の結果

日報DBにあった棋譜11局のうち3局で確認した。

| 棋譜 | 判定 |
|---|---|
| wakame_han(837) vs :Go9Bot(985) | 黒番 / 負 / -40.0目 / 終局 / 74手 |
| :OyajiBot(843) vs wakame_han(897) | 白番 / 勝 / 投了 / 40手 |
| DivineOrange77(1035) vs wakame_han(824) | 白番 / 負 / 投了 / 47手 |

- 自分の手番（黒・白の両方）、投了と終局の区別、相手レーティングの分離はすべて正しい
- 取られた後の同一座標への打ち直しが5件含まれていたが、正常系として処理できている
- 囲碁クエストのSGFには `DT` が無いため、対局日は日報DBの「日付」プロパティを使う

### 移行の本実行結果（2026-08-20）

日報DBの271ページを全走査し、SGFを含む11局すべてを「囲碁 棋譜DB」へ新規ページとして作成した。

```
移行した棋譜: 11 件
重複スキップ: 0 件
SGF が見つからずスキップ: 260 件（業務日報・ミーティング記録など、Go対局と無関係のページ）
エラー: 0 件
```

エラー0件だったことから、integration（`go-review-app`）が棋譜DB・学習ログDB・日報DBのいずれにも
問題なく接続済みであることも合わせて確認できた。

## 残り（手作業が必要）

1. 囲碁クエストの共有先を「囲碁 棋譜DB」へ切り替える
2. 3〜5日運用し、新しい対局が棋譜DBに正しく取り込まれる（取りこぼし・重複がない）ことを確認する
3. 安定が確認できたら、日報DBから `go-review-app` インテグレーションの接続を解除する
   （棋譜DBと学習ログDBは恒久的に接続したままにする）

旧DB（日報）は当面残す。共有先を間違えたときの受け皿として `sync` が監視し、
棋譜を見つけたら取り込んだうえでログに「旧DBに追加されています」と出す。
